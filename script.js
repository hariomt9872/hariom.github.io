// Simple JavaScript to handle any interactions or animations if needed
document.addEventListener('DOMContentLoaded', () => {
    // Example: Display an alert when the page is loaded
    console.log("Portfolio loaded successfully!");
});
